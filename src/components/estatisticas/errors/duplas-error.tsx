import { Card, CardContent } from "@/components/ui/card";

export default function DuplasError() {
  return (
    <Card>
      <CardContent>
        <p>Erro ao calcular as duplas.</p>
      </CardContent>
    </Card>
  )
}